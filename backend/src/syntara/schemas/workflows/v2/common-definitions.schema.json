{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://automation.example.com/schemas/workflows/v2/common-definitions",
  "title": "Common Schema Definitions",
  "description": "Shared schema definitions for workflow v2",
  "$defs": {
    "credential_id": {
      "type": "string",
      "description": "Syntara credential UUID for authentication. When provided, the credential is resolved at execution time and injected into the node config.",
      "format": "uuid"
    },
    "retry_policy": {
      "type": "object",
      "description": "Retry configuration for failed node execution. Only applies to executor and approval nodes. Unset fields inherit from global settings. Set max_retries to 0 to explicitly disable retry.",
      "properties": {
        "max_retries": {
          "type": "integer",
          "description": "Number of retries after the initial attempt. 0 = no retry. Inherits from global settings when absent.",
          "minimum": 0
        },
        "initial_interval": {
          "type": "integer",
          "description": "Initial retry interval in seconds. Inherits from global settings when absent.",
          "minimum": 1
        },
        "max_interval": {
          "type": "integer",
          "description": "Maximum retry interval in seconds. Inherits from global settings when absent.",
          "minimum": 1
        },
        "backoff_coefficient": {
          "type": "number",
          "description": "Multiplier applied to the interval after each retry. 1.0 = fixed interval (no growth). Values above 1.0 give exponential backoff. Inherits from global settings when absent.",
          "minimum": 1.0
        }
      },
      "additionalProperties": false
    },
    "nodeSettingsCof": {
      "type": "object",
      "description": "Settings with continue_on_failure only (converge, loop).",
      "properties": {
        "continue_on_failure": {
          "type": "boolean",
          "description": "When true, downstream nodes continue executing even if this node fails. Inherits from global settings when absent."
        }
      },
      "additionalProperties": false
    },
    "nodeSettingsNoRetry": {
      "type": "object",
      "description": "Settings with disabled, continue_on_failure, and timeout (script, agentic, approval). No retry_policy — retry is either not applicable or handled internally by the node's client.",
      "properties": {
        "disabled": {
          "type": "boolean",
          "description": "When true, the node is skipped during execution. Downstream nodes still execute but receive no output from this node."
        },
        "continue_on_failure": {
          "type": "boolean",
          "description": "When true, downstream nodes continue executing even if this node fails. Inherits from global settings when absent."
        },
        "timeout": {
          "type": "integer",
          "minimum": 1,
          "description": "Timeout in seconds."
        }
      },
      "additionalProperties": false
    },
    "nodeSettingsFull": {
      "type": "object",
      "description": "Full settings with continue_on_failure, timeout, and retry_policy (http_request, aap_job_template, aap_workflow_job_template).",
      "properties": {
        "disabled": {
          "type": "boolean",
          "description": "When true, the node is skipped during execution. Downstream nodes still execute but receive no output from this node."
        },
        "continue_on_failure": {
          "type": "boolean",
          "description": "When true, downstream nodes continue executing even if this node fails. Inherits from global settings when absent."
        },
        "timeout": {
          "type": "integer",
          "minimum": 1,
          "description": "Timeout in seconds."
        },
        "retry_policy": {
          "$ref": "#/$defs/retry_policy"
        }
      },
      "additionalProperties": false
    }
  }
}
