"""Add expected_duration and stall_alert_at to activity_execution.

Revision ID: e3a1f5b7d924
Revises: d5b8c2f04e71
Create Date: 2026-09-23 12:00:00.000000

Adds two nullable columns to support stall detection (AAP-92824):

- ``expected_duration`` (integer, seconds): resolved from node settings at
  dispatch time so the periodic stall scanner can evaluate without joining
  back to the workflow definition.
- ``stall_alert_at`` (timestamptz): written once by the StallDetectionWorker
  on first stall detection; acts as a dedup flag so telemetry fires exactly
  once per stall occurrence. Never cleared after node termination.

Both columns are nullable — existing rows default to NULL (never stalled).
"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op

# revision identifiers, used by Alembic.
revision: str = "e3a1f5b7d924"
down_revision: str | Sequence[str] | None = "d5b8c2f04e71"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.add_column(
        "activity_execution",
        sa.Column("expected_duration", sa.Integer(), nullable=True),
    )
    op.add_column(
        "activity_execution",
        sa.Column("stall_alert_at", sa.DateTime(timezone=True), nullable=True),
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_column("activity_execution", "stall_alert_at")
    op.drop_column("activity_execution", "expected_duration")
    # ### end Alembic commands ###
