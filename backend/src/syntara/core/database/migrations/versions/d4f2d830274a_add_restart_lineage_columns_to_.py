"""add restart lineage columns to executions

Revision ID: d4f2d830274a
Revises: c4a7e1f93d21
Create Date: 2026-09-15 17:46:13.084780

"""

from collections.abc import Sequence

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

# revision identifiers, used by Alembic.
revision: str = "d4f2d830274a"
down_revision: str | Sequence[str] | None = "d5b8c2f04e71"
branch_labels: str | Sequence[str] | None = None
depends_on: str | Sequence[str] | None = None


def upgrade() -> None:
    """Upgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    # NOTE: autogenerate also emits approval_requests ops here (prompt type,
    # loop_iteration_path, temporal_activity_id, unique constraint). Those
    # belong to un-migrated model changes from #478 and are intentionally left
    # out of this migration — they must ship with their own revision.
    op.add_column("executions", sa.Column("source_execution_id", sa.Uuid(), nullable=True))
    op.add_column(
        "executions",
        sa.Column(
            "failed_node_ids",
            postgresql.JSONB(astext_type=sa.Text()),
            server_default=sa.text("'[]'::jsonb"),
            nullable=False,
        ),
    )
    op.add_column("executions", sa.Column("triggered_by", sa.Uuid(), nullable=True))
    op.add_column("executions", sa.Column("restart_count", sa.Integer(), server_default=sa.text("0"), nullable=False))
    op.create_index(op.f("ix_executions_source_execution_id"), "executions", ["source_execution_id"], unique=False)
    # CUSTOM: explicit FK name so downgrade can drop it (autogenerate emits None).
    op.create_foreign_key(
        "fk_executions_source_execution_id",
        "executions",
        "executions",
        ["source_execution_id"],
        ["id"],
        ondelete="SET NULL",
    )
    # END CUSTOM
    # ### end Alembic commands ###


def downgrade() -> None:
    """Downgrade schema."""
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_constraint("fk_executions_source_execution_id", "executions", type_="foreignkey")
    op.drop_index(op.f("ix_executions_source_execution_id"), table_name="executions")
    op.drop_column("executions", "restart_count")
    op.drop_column("executions", "triggered_by")
    op.drop_column("executions", "failed_node_ids")
    op.drop_column("executions", "source_execution_id")
    # ### end Alembic commands ###
