"""Forms domain router placeholder.

The forms domain contains only shared schema definitions (FormDefinition, field types)
referenced by other domains. No API endpoints are exposed from this domain.
"""
