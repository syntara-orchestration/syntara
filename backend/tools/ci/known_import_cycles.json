{
  "description": "Known import cycle edges — accepted as permanent. auth.__init__ re-exports are public API consumed by 20+ modules. csrf.py uses a lazy import from dependencies.py. See docs/standards/static-analysis.md.",
  "known_edges": [
    ["syntara.auth.__init__", "syntara.auth.csrf"],
    ["syntara.auth.__init__", "syntara.auth.dependencies"],
    ["syntara.auth.csrf", "syntara.auth.dependencies"]
  ]
}
