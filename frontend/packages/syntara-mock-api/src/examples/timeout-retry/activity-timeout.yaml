---
schema_version: '2.0.0'
name: timeout-test
description: Test activity timeout

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: slow_task
    name: Slow Task
    type: script
    parameters:
      language: bash
      code: |
        sleep 10
        echo "This should not complete"
    timeout: 2
edges: []
