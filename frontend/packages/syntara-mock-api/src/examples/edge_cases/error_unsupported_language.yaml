---
schema_version: '2.0.0'
name: error-unsupported-language
description: Test error when task uses unsupported script language

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: python_task
    name: Task With Unsupported Language
    type: script
    parameters:
      language: python
      code: |
        print("This should fail")
edges: []
