---
schema_version: '2.0.0'
name: script-failure-test
description: Test script execution failure

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: failing_script
    name: Script That Fails
    type: script
    parameters:
      language: bash
      code: |
        echo "This script will fail" >&2
        exit 1
    outputs:
      result: $.stdout
edges: []
