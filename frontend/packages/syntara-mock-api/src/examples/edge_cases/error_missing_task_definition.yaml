---
schema_version: "2.0.0"
name: error-missing-task-definition
description: Test error when task activity has no task definition

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: invalid_task
    name: Task Without Task Definition
    type: task
    # Missing 'task' field - should raise ValueError
edges: []
