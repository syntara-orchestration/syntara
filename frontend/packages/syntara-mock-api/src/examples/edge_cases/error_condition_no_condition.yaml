---
schema_version: '2.0.0'
name: error-condition-no-condition
description: Test error when condition activity has no condition field

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: invalid_condition
    name: Condition Without Condition Field
    type: condition
    # Missing 'condition' field - should raise ValueError
  - id: then_task

    type: script
    parameters:
      language: bash
      code: echo "Then branch"
edges:
  - source: invalid_condition
    target: then_task
    port: then
