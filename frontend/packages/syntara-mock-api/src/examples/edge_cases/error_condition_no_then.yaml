---
schema_version: '2.0.0'
name: error-condition-no-then
description: Test error when condition activity has no then branch

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: invalid_condition
    name: Condition Without Then Branch
    type: condition
    condition: 'true'
    # Missing 'then' branch - should raise ValueError
edges: []
