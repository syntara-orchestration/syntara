---
schema_version: "2.0.0"
name: error-parallel-no-branches
description: Test error when parallel activity has no branches

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: invalid_parallel
    name: Parallel Without Branches
    type: parallel
    # Missing 'branches' field - should raise ValueError
edges: []
