---
schema_version: '2.0.0'
name: python-with-error
description: Python script that raises an exception to test error handling

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: failing_script
    type: script
    parameters:
      language: python
      code: |
        raise Exception("Intentional error for testing")
    retry_policy:
      maxAttempts: 3
      backoff: exponential
      initialInterval: 1
edges: []
