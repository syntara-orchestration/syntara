---
schema_version: "2.0.0"
name: python-timeout
description: Python script that sleeps to test timeout handling

triggers:
  - id: trigger_manual
    type: manual_trigger

nodes:
  - id: slow_script
    type: script
    parameters:
      language: python
      code: |
        import time
        time.sleep(10)  # Sleep longer than timeout
        print("This should not print")
    timeout: 1
edges: []
