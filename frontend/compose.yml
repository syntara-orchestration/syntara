---
services:
  syntara-mock-api:
    container_name: syntara-mock-api
    image: localhost/syntara-mock-api:latest
    build:
      context: .
      dockerfile: packages/syntara-mock-api/Containerfile
    ports:
      - 3000:3000
    environment:
      PORT: 3000
    restart: always
    networks:
      - syntara-net

  syntara-ui:
    container_name: syntara-ui
    image: localhost/syntara-ui:latest
    build:
      context: .
      dockerfile: packages/syntara-ui/Containerfile
    ports:
      - 4000:80
    environment:
      VIRTUAL_HOST: ui.example.com
      VIRTUAL_PORT: 80
      LETSENCRYPT_HOST: ui.example.com
    restart: always
    networks:
      - syntara-net
    depends_on:
      - syntara-mock-api

  nginx-proxy:
    container_name: nginx-proxy
    image: docker.io/nginxproxy/nginx-proxy:1.7-alpine
    environment:
      TRUST_DOWNSTREAM_PROXY: 'false'
      NGINX_ENTRYPOINT_QUIET_LOGS: 1
    ports:
      - 80:80
      - 443:443
    volumes:
      - certs:/etc/nginx/certs:ro
      - html:/usr/share/nginx/html
      - /var/run/docker.sock:/tmp/docker.sock:ro
    networks:
      - syntara-net

  nginx-acme:
    container_name: nginx-acme
    image: docker.io/nginxproxy/acme-companion:latest
    environment:
      DEFAULT_EMAIL: user@example.com
      NGINX_PROXY_CONTAINER: nginx-proxy
      NGINX_ENTRYPOINT_QUIET_LOGS: 1
    restart: always
    volumes:
      - certs:/etc/nginx/certs
      - html:/usr/share/nginx/html
      - acme:/etc/acme.sh
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - syntara-net
    depends_on:
      - nginx-proxy

volumes:
  certs:
  acme:
  html:

networks:
  syntara-net:
    driver: bridge
