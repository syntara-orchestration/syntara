---
description: PatternFly UX Design System — Opinionated implementation rules for building frontend UI that adheres to PatternFly standards and this project's UX team's component usage guidelines.
globs: ['packages/syntara-ui/src/**/*.tsx', 'packages/syntara-ui/src/**/*.ts']
alwaysApply: false
---

<!--
  SYNC NOTE: This is a condensed version of .claude/skills/frontend-patternfly-ux/SKILL.md
  (the comprehensive Claude skill). Both files must stay in sync — when updating one,
  update the other. This file is optimized for Cursor (auto-loaded, lightweight).
  The skill file is the source of truth for full details.
-->

# PatternFly UX Design System — Opinionated Implementation

When building frontend UI in this project, follow these opinionated PatternFly patterns. This ensures consistency across feature teams and reduces cognitive load for users.

## Philosophy

Our UX is designed around the **supervisor** mental model — an operator who must understand, trust, and intervene in complex automation across scale.

- **Standardized compositions** — Combine atomic PatternFly components into larger, prescribed compositions (e.g., a "complete table view" with specific pagination, filtering, and bulk action patterns). These compositions are the unit of consistency.
- **Data-driven adjustments** — Use side-out panels instead of modals for step configuration to preserve workflow canvas context.
- **No custom one-offs** — When PatternFly does not meet a requirement, collaborate via the PatternFly liaison path. Never build a custom component.

## Design System

How the UI is anchored:

- **Foundation** — Built on top of [PatternFly](https://www.patternfly.org/) for components, patterns, and accessibility baselines
- **Layout** — Page and shell structure follow PatternFly's **Compass** layout architecture
- **Theming** — Visual treatment uses PatternFly's **Unified Theme**
- **Icons** — Use icons with the `RhUi` prefix (e.g., `RhUiAddIcon`, `RhUiEditIcon`), NOT standard PatternFly defaults
- **Automation builder** — Based on [React Flow](https://reactflow.dev/) with PatternFly as visual wrapper
- **Accessibility** — Must achieve [WCAG 2.1 AA](https://www.w3.org/WAI/WCAG2AA-Conformance) and [Section 508](https://www.section508.gov/) compliance
- **PatternFly gaps** — Check PF docs first → Raise with UX team → Engage PatternFly → Document with `patternfly-override` label → Resolve upstream
- **`Nx` prefix convention** — opinionated global components use the `Nx` prefix (e.g., `SynPage`, `SynPanel`, `NxConfirmationDialog`, `NxDetailList`) in `src/components/` organized by: `layout/`, `dialogs/`, `details/`, `tabs/`, `states/`. Use the `Nx*` wrapper, not the raw PF component.

## Page Layout (MANDATORY)

Every page must follow this structural hierarchy:

| Layer           | Component                   | Purpose                                 |
| --------------- | --------------------------- | --------------------------------------- |
| App Shell       | `Compass`                   | Overall application frame               |
| Navigation      | `AppDockedNav`              | Left sidebar with icons                 |
| Page Content    | `CompassContent` + `SynPage` | Main content area wrapper               |
| Page Header     | `SynPageHeader`              | Page title and actions                  |
| Content Frame   | `SynPanel`                   | `Panel` → `PanelMain` → `PanelMainBody` |
| Main Content    | Table / Canvas / Form       | Primary page content                    |
| Footer (tables) | Pagination component        | Navigation between table pages          |

### Page Layout Archetypes

| Archetype          | Structure                                                                                                     |
| ------------------ | ------------------------------------------------------------------------------------------------------------- |
| **List page**      | `SynPageHeader` (Create CTA) → `SynPanel isFullHeight` → `SynPanelContentStack variant="inset"` → filter + table |
| **Detail page**    | `SynPageBreadcrumbs` → `SynPageHeader` → `SynPanel isFullHeight` → `SynPanelContentStack` (default) → tabs        |
| **Form page**      | `SynPageBreadcrumbs` → `SynPageHeader` → `SynPanel isFullHeight footer={<ActionGroup>…}` → form body              |
| **Error in panel** | Same shell → `SynPageBody isCentered` + `ErrorState` inside `SynPanel`                                          |

**`NxListPanel`** — canonical list page implementation. Compound API: `NxListPanelView` (handles loading/error/empty/filter states), `NxListPanelToolbar` (wraps in `<fieldset disabled={isFetching}>` during refetch), `NxListPanelTable`. Use instead of manual `SynPanelContentStack` + `useQueryState` + three-state branching.

**Sticky form footer** — Form Save/Cancel buttons live in `SynPanel footer={<ActionGroup>…</ActionGroup>}`, NOT in the page header. `PanelMain` scrolls while footer stays pinned. Cancel uses `variant="link"`. Applies to Create/Edit forms, Configure Integration, Settings tabs. Does NOT apply to builder node editor panels or modals.

### Page Header Types

- **Main page header** — Left-aligned title, right-aligned actions
- **Details page header** — Left-aligned breadcrumbs (`SynPageBreadcrumbs`) + title; toolbar left→right: Switch → Edit (primary) → kebab
- **Form page header** — Left-aligned breadcrumbs + title; no action buttons in the header — Save and Cancel live in the `SynPanel` sticky footer

### Breadcrumbs

- Use `SynPageBreadcrumbs`; renders nothing when < 2 items; last item = current page (non-link); middle segments collapse at ≤768px. No CSS overrides.

### Structure Checklist

- Use `SynPage` as outer wrapper
- Use `SynPageHeader` for title and actions
- Use `StackItem isFilled` + `SynPanel isFullHeight` for content
- Primary action is the rightmost button
- Canvas views use `hasNoPadding`

## Navigation

Use a docked icon navigation (left sidebar) with PatternFly's flyout panels component.

- Hover on nav item (no children) → tooltip with label
- Hover on nav item (has children) → flyout panel with sub-items
- Click nav item (no children) → navigate to route
- Click nav item (has children) → navigate to first enabled child route
- Click flyout sub-item → navigate to route, **close flyout immediately**
- Mouse leaves flyout → close after 150ms grace period

## Tabs

- Tab labels: sentence case, clear and professional, no punctuation
- **`NxUrlTabs` API:** `basePath`, `defaultTab` (defaults to `"details"`), optional `validTabs`. URL is single source of truth — no local state. Tab panel content owns inner padding (`--pf-t--global--spacer--lg`).
- **Tab intro paragraphs:** For complex admin sections (Access Management tabs), add a `Content` paragraph above tab content explaining purpose + RBAC context. Spacing: `marginBottom: var(--pf-t--global--spacer--md)`.

## Filter Bar

- Use PatternFly's Attribute Search component
- Default filter type: "Keyword" search (`contains` filter on all content)
- Filter bar is visible when data exists or when filters are active
- Filter bar is hidden only when the resource type has never had data created
- Filter select dropdowns hide `SearchInput` when fewer than 10 static options; async filters always show search

## Table Component

- ALWAYS use `NxScrollableTableContainer` wrapper — this applies the standard table variant by default
- `NxScrollableTableContainer` does not set `variant="compact"` — the default (standard) variant is used for main data tables
- Use `variant="compact"` only for dense, supplementary tables (e.g., activity state tables inside panels) where space is constrained
- ALWAYS include `<Thead>` with column headers
- Actions column is rightmost (no column header label)
- All row actions live inside a kebab menu (⋮) — no direct buttons in the actions column. Use `NxKebabMenu` component with `actions` array: `{ key, title, onClick, isSeparator?, isDanger?, isAriaDisabled?, tooltipProps? }`. Per-row `aria-label` must be unique (e.g., `` `Actions for ${name}` ``).
- **Kebab labels:** Always `"Action + resource"` format (e.g., "Edit credential", "Delete credential") with icons via `IconLabel`; destructive items use `isDanger: true`
- Action order in kebab: non-destructive first, divider, destructive last
- On the details page header, same actions appear in a kebab; frequently used actions promoted to primary button with icon (e.g., `RhUiEditIcon` + "Edit credential")
- Columns should be sortable if applicable
- Clicking resource name navigates to details view
- "Created"/"Modified" columns: username (linked) + date together. Date format: `MMM DD, YYYY, H:MM:SS AM/PM` — always use `DateCell` / `formatDateTime()` from `dateUtils.ts` (never `toLocaleString` or PF `Timestamp`)
- Text-heavy columns (names, descriptions, emails, URLs) MUST use `<Truncate>` — ellipsis with tooltip on hover; `table-layout: fixed` for equal column distribution (do not opt out with `useFixedLayout={false}`)
- Expandable rows: pass `isExpandable` to `NxScrollableTableContainer`, add expand-all/collapse-all toggle in `<Thead>` via `expand` prop, use `ExpandableRowContent` for nested detail, compact gray `Label` for list data
- Footer/pagination: cursor-based via `NxScrollableTableContainer`'s `footer` prop (`TableFooterProps`)
  - Left: item count — `"5 workflows (of 100 total)"`
  - Right: Previous / Next buttons using API cursor tokens (`prev`, `next`) — no page numbers or per-page selector
  - Pass `{ content, prev, next, onPrev, onNext }` to the `footer` prop

## Form Component

- Use PatternFly's Basic Form component
- Left-aligned, one column, max-width of 600px
- Submit buttons in `SynPanel` sticky footer (not in the page header)
- Use typeahead for lists with 10+ options
- Use Read-only Clipboard Copy for system-pre-populated values
- Use Validated component for form validation
- Use Number Input for number fields
- Use popover help text on form field labels
- Use `HelperText` / `HelperTextItem` below inputs for brief contextual guidance (formats, ranges, constraints); popover is for longer descriptions; validation errors replace helper text when invalid
- Use Zod + react-hook-form for validation (project standard)
- **Dropdowns:** Never use native `<select>` or PF `FormSelect`. Use PF6 `Select` + `MenuToggle` + `SelectList` + `SelectOption`. In modals: `popperProps={{ appendTo: 'inline' }}`. Add `shouldFocusToggleOnSelect`.
- **Human-readable validation:** Never expose regex to users. Use plain-language errors + inline hint text via `HintOrError`/`HelperText`. Use example-style placeholders (e.g., `'my-project-name'`).
- **Read-only system values:** Never use disabled `TextInput` for non-editable values. Use `DescriptionList isCompact`, `ClipboardCopy`, or plain text instead.

## Typeahead Patterns

- **Project selector prefix label:** Visible `"Project:"` prefix in masthead typeahead toggle using `InputGroupItem` + `Content` with `--pf-t--global--text--color--subtle`. Don't rely on placeholder alone.
- **Multi-select typeahead with chips:** `MenuToggle variant="typeahead"` + `LabelGroup`/`Label color="blue"` for selected items. Filter-as-you-type with checkbox options, close (×) per chip, clear-all button. Create-only fields (e.g., group assignment) appear on Create form only; editing via dedicated detail panel.

## Details Component

- Use `NxDetailList` + `NxDetail` (vertical default; `isHorizontal` for compact contexts like canvas steps)
- `NxDetail` with empty/null children **renders nothing** — no manual null checks or placeholders needed
- Use `NxCodeBlock` for JSON/scripts/logs — supports `enableCopy`, `enableExpand`, `jsonObject`
- Consistent date/duration formatting per PatternFly Date/Time guidelines
- Header: title + primary actions from the table row
- **Plain text for informational metadata** — credential type, auth method, etc. are plain text, not `Label` badges. Use `Label` only for statuses or visual distinction.
- **Created/Modified in tables:** inline `UserTimestamp` — `username · date` on one line; stacked mode for detail views only.

## Empty States

Use PatternFly's Basic Empty State component. Empty states replace the main content area.

| Scenario          | Title                          | CTA                                 | Filter?                  |
| ----------------- | ------------------------------ | ----------------------------------- | ------------------------ |
| No data           | `"No [resources] yet"`         | `"Create [resource]"` if applicable | No                       |
| No filter results | `"No results found"`           | `"Clear all filters"`               | Yes, with active filters |
| Service error     | `"Unable to load [resources]"` | `"Retry"`                           | No                       |

Icons per scenario: No data → `PlusCircleIcon` (gray, `lg`), No results → `SearchIcon` (gray, `sm`), Error → `ExclamationCircleIcon` (`status="danger"`, `lg`), No access → `LockIcon` (gray, `lg`), Config required → `WrenchIcon` (gray, `lg`), Success → `CheckCircleIcon` (`status="success"`, default/`xl`), Getting started → `RocketIcon` (gray, `xl`). Use `status` prop for color on status empty states; non-status icons are gray by default. Variant sizing: `sm` in tables/modals, `lg` full-page, `xl` onboarding/success.

- **CTA deduplication:** When empty state has a create/configure CTA, hide the page-header primary button — it reappears once data exists.
- **Tab-level empty states:** Use shared `EmptyStateNoData` component with correct heading level (`h2` inside tabs) and `isFullHeight`.
- **Three-state list page pattern:** 1) Query error/loading → `useQueryState` renders loading or error; 2) No data AND no filters → `NxEmptyStateNoData` (hide FilterBar); 3) Data OR active filters → show FilterBar; empty with filters → `NxEmptyStateFilter`.
- **Access denied:** `EmptyStateAccessDenied` with `RhUiLockIcon` when user navigates directly to a page they can't read.

## CRUD Patterns

Action verb pairings: "Create" ↔ "Delete", "Add" ↔ "Remove" (internal resources), "Add" ↔ "Disconnect" (external resources), "Assign" ↔ "Unassign", "Transfer" for moving ownership between entities (e.g., "Transfer identity"), "Configure" for integrations (not "Add integration" — integrations are external connections being configured)

### Create

- **Full page:** 5+ fields, multi-step wizards
- **Modal:** 2–4 simple fields, quick creation without leaving context

### Read/Detail

- Full page for resources with rich information

### Update/Edit

- **Full page:** 5+ fields, multi-step, or if create is full page
  - Pre-populate fields, track dirty state, warn on unsaved navigation, show loading while saving
- **Modal:** 2–4 fields, or if create is a modal
- **Inline:** Single-field edits, toggles (use PatternFly Switch Checked with Label), renames

### Confirmation Dialog — Three-Tier Severity

`NxConfirmationDialog` supports three tiers: **Default/Disable** (primary, no icon) → **Danger** (danger + warning icon, no checkbox) → **DestructiveAcknowledgement** (danger + warning + required checkbox). See Storybook for canonical copy per tier.

### Delete (Destructive) — Three Variants

ALWAYS use `NxConfirmationDialog` from `src/components/dialogs/NxConfirmationDialog.tsx`. Never use raw `Modal` + `ModalHeader` + `ModalBody` + `ModalFooter` for delete actions.

All delete variants share: Small modal, warning icon title, `confirmVariant="danger"`, `confirmLabel="Delete"`, cancel `variant="link"`, checkbox via `destructiveAcknowledgement` (Delete button disabled until checked).

**Simple Delete** — no downstream effects (role, policy, group, user, identity provider)

- Body: `"The [resource] [name in bold] will be deleted. This cannot be undone."`
- Checkbox: `"I understand this [resource] will be permanently deleted."`

**Cascade Delete** — other records get permanently deleted too (workflow → executions, tool provider → tools)

- Body: `"The [resource] [name in bold] will be deleted. This cannot be undone."`
- Body 2: `"Resources that will be deleted"` as a header, then one row per resource type with its own Badge count — e.g., "Executions [12]", "Tools [3]"
- Checkbox: `"I understand this [resource] and the resources shown above will be permanently deleted."`

**Ripple Effect Delete** — other resources break/become invalid but are not deleted (credential → referencing workflows fail, project → orphaned resources, workflow → parent workflows become invalid)

- Body: `"The [resource] [name in bold] will be deleted. This cannot be undone."`
- Body 2: `"Resources that will be affected"` as a header, then one row per resource type with its own Badge count — e.g., "Workflows [2]", "Credentials [5]"
- Checkbox: `"I understand this [resource] and the resources shown above will be affected by this deletion."`
- When badge counts unavailable: use `Stack` + PF `List`/`ListItem` bullet consequences (never raw `<ul>`/`<li>` — enforced by `prefer-pf-list-components` ESLint rule)
- Extract one shared dialog component per destructive action type (e.g., `IdentityProviderDeleteDialog`) — consumed from list + detail views

> A resource can combine variants (e.g., workflow delete = cascade for executions + ripple for parent workflows). Show both Body 2 sections in that case.

- Post-delete: stay on list (from table), navigate back (from detail), show toast alert

### Remove/Unassign/Cancel/Stop (Destructive, Reversible)

- Modal variant: Small with warning icon
- Title: `"[Remove/Unassign/Cancel/Stop] [resource type]?"`
- Body: resource name and consequence
- Action button: `variant="danger"`
- Cancel button: `variant="link"`
- Post-cancel/stop: stay on list (from table), stay on details (from detail), show toast alert

### Disable (Non-Destructive)

- **Enable does not require confirmation** — takes effect immediately. Only **disable** requires a confirmation dialog.
- Use standard confirmation modal (no warning icon, no danger button)
- Modal variant: Small (PatternFly Small Variant Modal)
- Title: `"Disable [resource type]?"`
- Body: `"You are about to disable the [resource type] [name in bold]. You can re-enable the [resource type] at any time."`
- Confirm button: `variant="primary"`
- Cancel button: `variant="link"`
- Post-disable: stay on list (from table), stay on details (from detail), show toast alert

## Feedback & Notifications

### Success Feedback

- Use PatternFly's Dismissible Success Toast Alert for success messages after create, update, delete actions
- Toast alerts should auto-dismiss after a reasonable duration
- Message format: Title in sentence case, past tense — `"[Resource type] [past-tense action]"` (e.g., "Role created"). Description includes entity name.
- **Verb consistency:** Toast copy must match the action verb (button says "Create role" → toast says "Role created", not "Role added")
- **No toast when redundant:** Skip success toasts when UI already communicates outcome — inline Switch state change, navigation to new view, dirty state clearing. Error toasts always kept. Create actions still show toast.

### Error Feedback

- Page-level loading errors: use `useQueryState(query, { title: '...', onRetry: ... })` — hooks up `ErrorState` with retry automatically
- Mutation errors: use `useMutationErrorHandler` — wires up `ErrorState` and toast alerts automatically
- Form validation errors: use inline field-level errors via PatternFly's Validated component
- **Error state placement:** `ErrorState` renders inside `SynPanel` via `SynPageBody isCentered` — page header and shell remain visible.

### Loading States

- Use PatternFly's Spinner component during async operations
- Page-level: centered spinner in content area
- Button actions: loading spinner on the button, disable during operation
- Tables: skeleton or spinner while data loads

## Button Placement Rules

Button alignment differs by context (follows PatternFly convention):

| Context           | Primary action position                |
| ----------------- | -------------------------------------- |
| Page headers      | **Rightmost**                                         |
| Modals            | **Leftmost** (Cancel link on right)                   |
| Forms (full page) | **Leftmost** in SynPanel sticky footer (Cancel = link) |
| Toolbars          | **Leftmost** (kebab menu on far right)                |

## Modals

- Use PatternFly's Small Variant Modal
- Delete modals use Title Icon Modal
- Buttons: left-aligned, primary → secondary → tertiary
- Cancel button is `variant="link"` when paired with primary action
- This applies to all modals, including delete confirmations
- **Unsaved-changes modal:** Save [resource] (primary) → Exit without saving (secondary) → Cancel (link). Use specific labels like "Save workflow", not generic "Save".

## Bulk Actions

- Use PatternFly Selectable with Checkbox Table
- Expandable table column order: expand/collapse chevron → checkbox → table columns
- Bulk actions in kebab menu in toolbar (right of primary button)
- Delete/remove option is always last
- Always include bulk action confirmation modal
- **Exception — Header toolbar bulk actions (Approvals):** For high-frequency bulk decisions, use direct header toolbar buttons instead of kebab. Checkboxes on actionable rows only; disabled when user lacks permission. Selection persists across pagination, clears on filter/sort. Approve = optional note; Reject = required reason + `variant="danger"`.

## Statuses and Labels

- **Statuses:** PatternFly Outline Status Label component
- **System labels:** Use `NxLabel` (filled, compact by default) — defaults to gray; use `color` for categories, `status` for statuses
- **User-generated labels:** Use `NxUserTag` (outlined, compact) for any user-entered values (tags, filter chips)
- If labels on a table reference a resource, make them clickable, navigating to the details page

## Icons

All icons MUST use the **`RhUi` prefix** (e.g., `RhUiAddIcon`, `RhUiEditIcon`, `RhUiTrashIcon`). Do NOT use legacy PF icon names (`PlusCircleIcon`, `PencilAltIcon`, etc.) — enforced via ESLint `no-restricted-imports`. Exception: PF empty state icons (`PlusCircleIcon`, `SearchIcon`, etc.) are still used in `EmptyState` components.

## Expand/Collapse Chevrons

- Use PatternFly Expandable Table component

## Content Rules

- **Sentence case** by default everywhere
- **Title case** only for navigation items and page titles
- **User-generated strings** are displayed exactly as entered — do not transform casing

## Workflow Builder

- Based on React Flow with PatternFly as visual wrapper
- Canvas built left to right
- Single click: select/expand/collapse step
- Double click: open panel view with Input/Output info
- Right click: same as kebab menu
- **Toolbar hierarchy:** Primary actions visible (Add step, Run, Save, Publish); secondary in grouped kebab with `DropdownGroup` ("Views": Run history, Workflow details; "Actions": Verify, Duplicate, Export, Import, Delete with `isDanger`). Every kebab item has icon + label.
- **Publish lifecycle:** Draft → Publish → Unpublish (replaces enable/disable). Status badges (no icons, text + color only): Draft (grey), Published (green `status="success"`), Unpublished changes (yellow `status="warning"`). Publish dialog requires version name. Unpublish via kebab with danger confirmation.
- **Save behavior:** `isAriaDisabled` when `!isDirty` for existing workflows. Tooltip shows "Last saved {datetime}". No toast on update; toast on create only.
- Add step: opens side panel, auto-adjusts canvas. **Empty workflow onboarding:** When no triggers/steps exist, "Add step" button is **hidden** (not disabled) and the panel is forced open for trigger selection. Once first step is added, button appears. **Toggle-style button:** When panel is open (and steps exist), button uses `isClicked` + `aria-pressed` for toggle feedback.
- **Duplicate workflow:** Row kebab action with `RhUiDuplicateIcon`; no confirmation dialog; success toast with inline deep-link to new workflow.
- Run workflow: single trigger → plain Run button; multiple triggers → dropdown for trigger selection; confirmation dialog with "Don't show again" (stored in `localStorage` per user — skips confirmation on next run), then `RunWorkflowModal` JSON editor for mock trigger data
- **Schedule triggers:** Interval (`DurationInput`) and Cron (`TextInput` with 5-field validation). "Continuous" type removed. Both show "Next run" preview when valid.
- Test step (Run step): from step kebab menu → choice dialog ("Run all previous steps" or mock data) → mock data editor (CodeEditor with JSON); upstream steps mocked as "skipped"; results in execution visualizer
- **Cancel run:** secondary destructive button (`variant="secondary"` + `isDanger`), no confirmation dialog. Visible only when status is `pending`/`running`.
- **Canvas controls:** anchored to bottom-left corner; canvas overlays (controls, step legend, undo/redo) use `SynPanel` with `variant="raised"` for opaque + shadow
- Legend toggle: accessible labels **Show step legend** / **Hide step legend**
- **Canvas step styling:** workflow steps use `variant="raised"` with border-radius override to match `Card` / canvas chrome; fixed 240px width — use `overflow-wrap: anywhere` on all dynamic text (`Title`, `Content`) to prevent overflow (prefer `anywhere` over `break-word`)
- **Floating canvas panels:** prefer `SynPanel` with `variant="raised"` for compact controls or `opaqueFloatingFill` for large flat shells without raised chrome (see `SynPanel.tsx` JSDoc)
- Execution view panels: use `SynPanel isFullHeight` for proper scroll — don't hand-roll flex column inline styles
- View-only views: Run view, Run history, Version history
- **Verify workflow:** Kebab action → `POST /workflows/validate` → expandable `Alert` (`ValidationBanner`) above canvas + per-node error badges. Node names are clickable links to the node editor; errors grouped via compact horizontal `DescriptionList`.
- **Two-tier validation:** Errors (`severity: 'error'`) block publish; warnings (`severity: 'warning'`) allow save via `force_save=true` retry. Banner variant: any error → `danger`; all warnings → `warning`. Verify-then-publish gate: Publish always verifies first; `isAriaDisabled` when errors exist or verifying.
- **Version history panel:** Right-side `SynPanel isFullHeight` with `SimpleList` grouped by date. Each version shows name + timestamp + status badge (Published/Draft). Kebab per version: Restore, Export, Publish. Viewing a version sets canvas to read-only mode. Mutually exclusive with approval panel.
- **Three-column node editor:** Input (flat) | Parameters (`variant="raised"`) | Output (flat). Branching nodes add branch-handle dropdown in center column header. Columns use `ResizableDivider`.
- **Node settings:** `NodeFormTabsLayout` splits node forms into Parameters + Settings tabs. Settings tab: continue-on-failure (three-way select), timeout, retry via shared `NodeSettingsForm`. System defaults as live placeholders from `/settings`. Hidden for Condition/Switch/triggers.
- **Node panel navigation:** Previous/Next arrow controls on panel edges for navigating connected steps. Single target = icon button + tooltip; multiple = dropdown menu. Uses `useAdjacentNodes`.
- **Node disable/enable:** Activity nodes (Task, Approval) get Disable/Enable kebab action (no confirmation). Disabled = dashed gray border + 50% opacity. Control flow nodes only get Replace + Delete.
- **Switch node expressions:** Paths share `ExpressionBuilderCore` (AND/OR groups + custom expression toggle). Drag-reorder paths via `@dnd-kit/sortable` with grip handle.
- **Approval side panel:** Right-side panel on execution viewer for inline approval review. Mutually exclusive with history panel; auto-opens on pending approval via `useAutoApprovalDetection`. **Multi-approval navigation:** `ApprovalNavigationHeader` with prev/next arrows, "2 of 5" counter, keyboard nav support.
- **Wait node countdown:** Live `HH:MM:SS` countdown on canvas during execution via `useWaitCountdown` (1s interval). Clears on terminal status.
- **Import confirmation:** Modal with radio choices ("Import as new" vs "Import into current") for existing saved workflows. Direct import for new/empty. Warns about unsaved changes when `isDirty`.

## Permission Gating

Every domain uses a `use{Domain}Permissions()` hook returning `{ can*, isLoading, tooltips }`. Default to **deny** while loading (safe-false). Always use `isAriaDisabled` (not `isDisabled`) — keeps elements focusable for tooltips/screen readers. Set `onClick={undefined}` when denied (defense in depth).

**Gating strategy:**

- **Nav items:** Hide via `requiredPermissions` (OR logic — visible if any granted)
- **Tabs:** Filter with `NxUrlTabs validTabs`; show all during load; redirect invalid URLs to first visible tab
- **Toolbar buttons:** `DisabledWithTooltip` wrapper + `isAriaDisabled` + permission tooltip
- **Row/kebab actions:** `isAriaDisabled` + `tooltipProps` on action items
- **Empty state CTA:** Pass `undefined` callback to hide button entirely
- **Create/edit routes:** `ProtectedRoute` → `EmptyStateAccessDenied` on direct URL
- **Read-only mode (builder):** Info banner + hide editing affordances + canvas lockdown (`nodesDraggable={false}`, `nodesConnectable={false}`)

Permission tooltip format: `"To {action}, you need a role with the {policy} policy. Contact your Admin to request access."`

## Session Timeout Warning

Non-dismissible alert dialog for security-critical time warnings: `Modal variant="small" role="alertdialog"` with `onClose={undefined}`. Live countdown with `aria-live="assertive"`. Actions: "Continue session" (primary) + "Log out" (link). Centralize timing constants in a constants file.

## Integration Configuration

- 3-step PatternFly Wizard for initial setup: Select type → Configure connection → Review & confirm with "Test connection"
- Detail page header: integration name + enabled/disabled `Switch` toggle (immediate, no confirmation for enable)
- "Test connection" secondary button — must pass before enabling tools
- Tools tab: table of available tools with per-row `Switch` to enable/disable individual tools

## Access Management & My Profile

- Access Management tab label: **"Check access"** (not "Can I")
- Self-service flows (Check access, My tokens) live on the **My Profile** page, not Access Management
- Access Management contains only admin tabs: Users, Groups, Projects, Roles, Policies, Assignments
- **My Profile** (`/my-profile`): reuses `UserDetail` with `isMyProfile` flag; no breadcrumbs; extra tabs (My tokens, Check access); accessible from user avatar menu in masthead

## Full-Page Wizard

Use when flow has 2+ steps with tables/filters (too large for modal). Dedicated route. Each step: title + description (bold target entity) + FilterBar + ScrollableTableContainer (radio selection). Footer: Back/Next/Cancel (link). Cancel navigates to origin route. Going back clears selections. Terminology must be consistent across button/loading/toast/error.

## Dedicated Edit Page

Use when editing is too complex for inline (many rows, nested modals, external actions). Parent tab stays read-only with "Edit" button navigating to edit route. Edit page: `SynPageHeader` with breadcrumbs + `SynPanel` with Save/Cancel in sticky footer. Permission-gated with `EmptyStateAccessDenied` fallback.

## Accessibility Guidelines

Achieving full WCAG 2.1 AA and Section 508 compliance requires careful implementation beyond PatternFly's built-in component accessibility. Developers are responsible for contextual accessibility:

### Semantic HTML & Page Structure

- Proper heading hierarchy (`<h1>`–`<h6>`) without skipping levels
- Use semantic tags (`<main>`, `<nav>`, `<header>`, `<footer>`, `<aside>`) or ARIA landmark roles

### Keyboard Navigation & Focus Management

- Every interactive element must be fully keyboard-operable
- Never remove focus outline without providing a visible alternative (3:1 contrast ratio)
- Manage focus programmatically on route changes and modal open/close
- Trap focus inside modals until dismissed; return focus to trigger on close

### Dynamic Content & State Changes

- Use `aria-live` (`polite` or `assertive`) for dynamic updates (job status, toasts)
- Use `aria-expanded`, `aria-disabled`, `aria-current` for state communication

### Forms & Error Handling

- Every input must have an associated `<label>` (not just placeholder text)
- Use `aria-invalid="true"` and `aria-describedby` to link inputs to error messages

### Color, Contrast, and Iconography

- Text and icons: 4.5:1 contrast for normal text, 3:1 for large text/UI components
- Never convey information by color alone — include icons and descriptive text
- Use `alt=""` or `aria-hidden="true"` for decorative images

### Testing

- Automated: accessibility tools in unit tests and CI/CD
- Manual: keyboard-only testing with Tab, Shift+Tab, Enter, Space, Arrow keys
- Screen reader: verify actual user experience with assistive technology

## Styling Rules

### No Global, Unscoped CSS

NEVER write global CSS rules that target element types, PatternFly class names, or broad selectors. Global styles bleed across component boundaries, silently override design tokens, and break theming and upgrade-compatibility.

```css
/* ❌ BAD */
.pf-v6-c-menu__item {
  min-height: 0;
}
p {
  margin-bottom: 8px;
}

/* ✅ GOOD — scoped CSS Module */
/* MyComponent.module.css */
.menuItem {
  min-height: 0;
}
```

### Styling Priority Order

1. **PatternFly props/variants** — use built-in props (`variant`, `isCompact`, `hasNoPadding`, etc.) first
2. **PF6 design tokens** — use `var(--pf-t--global--*)` for spacing, color, sizing; never hardcoded `px`
3. **CSS Modules** (`.module.css`) — scoped overrides only; never use `:global()` inside a module
4. **Inline `style` prop** — only for dynamic values computed at runtime

### Semantic Tokens Only

- **No hard-coded border colors** — use `--pf-t--global--border--color--default` and `--pf-t--global--border--width--divider--default`. Never custom rgba hex on table/layout components.
- **No breadcrumb CSS overrides** — PF6 default dashed underline and link colors are correct.
- **Compact inline form controls** — use PF spacer tokens (e.g., `--pf-t--global--spacer--4xl`) in CSS modules for explicit widths. Use `flexWrap: nowrap` + `flex: none` to prevent collapse.

### Exceptions

If a global style seems necessary, follow the PatternFly gaps process: check PF docs/tokens → raise with UX → engage PatternFly upstream. Approved exceptions must be documented with a `patternfly-override` issue label.

## Storybook

- `npm run storybook` (port 5174). Preview in light + dark mode via toolbar before sign-off.
- Stories reflect real app compositions, not isolated prop demos.
- `Nx*` components with `autodocs`: `SynPage`, `SynPageHeader`, `SynPageBreadcrumbs`, `SynPanel`, `SynPanelContentStack`, `NxUrlTabs`, `NxConfirmationDialog`, `NxCodeBlock`, `NxDetail`, `NxDetailList`, `NxErrorState`, `NxLoadingState`, `NxEmptyStateNoData`, `NxEmptyStateFilter`, `NxEmptyStateServiceUnavailable`, `NxListPanel`, `NxKebabMenu`, `NxLabel`, `NxUserTag`, `NxScrollableTableContainer`.
